---
"@infracraft/gate": minor
---

Initial release: multi-account switcher for Vercel, Railway, and Fly.io with real native-CLI switching (`gate vercel switch`, `gate railway switch`, `gate fly auth switch`) and metadata-free sandboxed deploys (`gate vercel deploy`, `gate railway up`, `gate fly deploy`). Supersedes vergate.
